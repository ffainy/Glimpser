{
  "name": "glimpser",
  "version": "0.0.3",
  "description": "Browser extension to preview links in a Modal iframe via drag-and-drop",
  "type": "module",
  "scripts": {
    "test": "vitest --run --passWithNoTests",
    "test:watch": "vitest",
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "crx": "^5.0.1",
    "fast-check": "^3.22.0",
    "jsdom": "^25.0.0",
    "vitest": "^2.1.0"
  }
}
